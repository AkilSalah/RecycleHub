import { TotalWeightPipePipe } from './total-weight-pipe.pipe';

describe('TotalWeightPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalWeightPipePipe();
    expect(pipe).toBeTruthy();
  });
});
